# Crawling, planner core, and web server all run on standard library only.
# Attachment analysis (M3-B) is the only feature that needs external packages.
pymupdf>=1.24
# At least one LLM provider must be installed.
# Choose the one matching your LLM_PROVIDER env var.
anthropic>=0.50
google-genai>=1.0
